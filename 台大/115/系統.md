1. computer system
	1. [ANS: ABC]
		1. safety-critical system : prefer deterministic schedule, improve worst-case performance
2. process & thread
	1. [ANS: BD]
		1. thread belong to same process
			1. share
				1. same data section
					1. global variable
					2. static variable
				2. heap memory
				3. text section (code)
			2. DO NOT share 
				1. program counter(PC) , because each thread may be executing different instruction simultaneously
				2. stack (for local variables, function calls, return addresses)
		2. context switch
			1. thread is faster than processing (ChatGPT 提供，不一定對)
				1. thread share same address space
				2. no need to switch page table
				3. less state need to be saved/restored
		3. synchronous threading
			1. parent thread wait(join) for child thread to finish before continuing
			2. asynchronous threading : parent thread create a child and resume its execution
3. virtual memory
	1. [ANS: ABCDE]
	2. type
		1. pure demand paging : 可以 start with no page loaded, so first instruction fetch can page fault
		2. opt page replacement 需要 future knowledge，所以不能 implement in pratical
		3. page replacement algo. that satisfy the stack property cannot suffer from Belady's anomaly (投影片有但不知道為什麼)
		4. 