1. dynamic loading
	1. [ANS: AB]
		1. routine loaded until it is called, relocatable file 
2. soft trap / hardware interrupt
	1. [ANS: AC]
3. single-level v.s. multi-level page table 
	1. [ANS: CD]
4. multi-thread process ( fork() & exec() )
	1. [ANS: BC]
5. fd
	1. [ANS: CD]
6. multi-thread process (fork() & exec())
	1. [ANS: BC]
	2. fork() 的 lock 會繼承到 child process ?
	3. Q : fork() 跟 exec() 在 multi-thread 的比較
7. fd
	1. [ANS: ACD]
	2. trashing : CPU I/O 頻繁 page 
	3. Belady anomo
	4. fork() : CoW (Copy on Write)
	5. Working-Set model
8. 
	2. trashing : 頻繁 page faualt，CPU 大量時間在等 paging I/O，ready queue 變小，CPU utilization 下降，OS 可能誤判 CPU 不夠忙，增加 multiprogramming $\rightarrow$ trashing 更嚴重
	3. Belady's anomaly : increase page frame but increase number of page faults (可以用數學證明)
		1. 會發生 : FIFO
		2. 不會發生 : stack implemented algo. (LRU)
	4. Copy-on-Write (COW) : 
9. 
