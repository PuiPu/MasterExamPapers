1. thread/process [ANS: CD]
	1. thread 
		1. shared : code segment, global variable, heap, open files
		2. not shared : stack, program conunter, register
	2. kernel thread 不會 share program counter
		1. 任何的 user 或是 kernel thread 都不會共享 thread
	3. many-to-one model
		1. 如果多個 user-level thread 都對應到同一個 kernel thread, 那就沒辦法發揮 multi-core CPU 的優勢
	4. many-to one model
		1. race condition 會發生在 multiple user threads mapping to single kernel thread
2. 