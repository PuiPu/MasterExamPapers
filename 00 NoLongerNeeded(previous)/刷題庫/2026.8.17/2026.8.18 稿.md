先從 Process 介紹起，OS 就是介於硬體跟 user 之間，control & coordinate(協調) 資源。

# process
process : 執行中的 program (放在 memory)
(放 process layout 的圖)
program : (放在 disk)

thread 有自己的 (1) program counter (2) registers (3) stack


interrupt I/O


UNIX 有支援三個 operation
1. fork()
2. execlp()
3. wait()

介紹 fork() 的例子...

# multi-process/thread
可不可以 mutliprocessor/multithreading
多個 thread 可不可以同時跑
single-core $\rightarrow$ concurrency 
multi-core $\rightarrow$ parallelism
(會影響到後來 synchronization 問題)

# process communication
process 之間怎麼溝通
IPC
1. shared memory
2. message passing

# 


